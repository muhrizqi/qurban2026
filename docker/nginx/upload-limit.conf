# File ini sengaja dikosongkan.
# Konfigurasi client_max_body_size diatur via ENV NGINX_CLIENT_MAX_BODY_SIZE
# di dalam Dockerfile, karena image serversideup/php sudah menyediakan
# ENV variable tersebut untuk menghindari duplikat directive.
